
public class Clazz {
	
	//spring framework	
		// if too many objects, memory leak problem will occur		
		// avoid unnecessary objects		
		// if same object created once and once again		
		// then use one only
	public void callMe(){
		System.out.println("call");
	}

}
